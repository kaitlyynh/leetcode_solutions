class Solution(object):
    def searchMatrix(self, matrix, target):
        """
        :type matrix: List[List[int]]
        :type target: int
        :rtype: bool
        """
        for row in matrix:
            if target <= row[-1]:
                for num in row:
                    if num == target:
                        return True
        return False
